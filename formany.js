 // baza.findById(response.clientId).then((client) => {
                //     console.log('c', client);
                // });

                // console.log('adres do szukajki: ', response.route, response.streetNumber, response.locality)

                // const set = {};
                // if (response.manyAddresses) {
                //     manyAddresses.forEach((address, index) => {
                //         set[`addresses.${index}.coordinates`] = address.coordinates;
                //     });
                // }else{
                //     set[`addresses.0.coordinates`] = 
                // }

                // baza.findByIdAndUpdate(response.clientId, { $set: set }, { new: true }, function (err, client) {
                //     res.status(200).send({
                //         success: true,
                //         'client': client
                //     });
                // });